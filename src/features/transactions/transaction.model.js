// transaction.model.js (Prisma)
// All transaction DB logic should use the Prisma client.
const { prisma } = require("../../core/config/db");

// Example: createTransaction, getTransactions, deleteTransaction, etc.
// Implement your transaction DB functions here using Prisma.

module.exports = {
  // Example:
  // createTransaction: async (data) => await prisma.transaction.create({ data }),
  // getTransactions: async (userId) => await prisma.transaction.findMany({ where: { userId } }),
  // deleteTransaction: async (id) => await prisma.transaction.delete({ where: { id } }),
};
