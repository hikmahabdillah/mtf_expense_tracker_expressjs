// user.model.js (Prisma)
// All user DB logic should use the Prisma client.
const { prisma } = require("../../core/config/db");

// Example: createUser, findUserByUsername, etc.
// Implement your user DB functions here using Prisma.

module.exports = {
  // Example:
  // createUser: async (data) => await prisma.user.create({ data }),
  // findUserByUsername: async (username) => await prisma.user.findUnique({ where: { username } }),
};
